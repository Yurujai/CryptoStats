<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-menu">
    <a class="navbar-brand" href="#">{{ "Crypto Stats Platform"|trans }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item {% if app.request.get('_route') == 'crypto_stats' %}active{% endif %}">
                <a class="nav-link" href="{{ path('crypto_stats') }}">
                    {{ "Home"|trans }}
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('investment') }}" class="nav-link">
                    {{ "Investment"|trans }}
                </a>
            </li>
            <li class="nav-item">
                <a href="#" title="{{ "Update wallets"|trans }}" class="nav-link update-element" data-url="{{ path('crypto_stats_update_wallets') }}">
                    {{ "Update wallets"|trans }}
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('crypto_stats_remove_wallets') }}"  title="{{ "Remove wallets"|trans }}" class="nav-link">
                    {{ "Remove wallets"|trans }}
                </a>
            </li>
        </ul>
    </div>
</nav>
