{% extends "base.html.twig" %}

{% block title %}{{ ("Crypto Stats - " ~ exchange|upper)|trans }}{% endblock %}

{% block body %}

    <div class="container-fluid">
        <div class="row">
            {% for item in balance|filter(item => item.amount|number_format(8) > 0) %}
                {% include "exchange/_crypto.html.twig" %}
            {% endfor %}
        </div>
    </div>

{% endblock %}
