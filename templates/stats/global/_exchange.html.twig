{% if is_exchange_enabled(exchange) %}
    <div class="col-xl-3 col-md-6 col-sm-6 mb-4">
        <div class="card border-left-success shadow h-80">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <a href="{{ path('crypto_stats_exchange_'~exchange) }}" target="_blank">
                            <div class="text-xs font-weight-bold text-success text-uppercase mb-1">
                                {{ exchange }}
                            </div>
                        </a>
                        <div class="h5 mb-0 font-weight-bold text-gray-800">
                            {{ globalUSD|number_format(2) }}$ / {{ globalEUR|number_format(2) }}€
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-money-bill fa-2x text-gray-300"></i>
                    </div>
                    <div class="update-exchange-wallet">
                        <a href="#" data-url="{{ path('crypto_stats_update_'~ exchange~'_wallets') }}" title="{{ "Update " ~ exchange ~ " wallets"|trans }}">
                            update
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endif %}
