{% extends "base.html.twig" %}

{% block title %}{{ "Crypto Stats - Kukoin"|trans }}{% endblock %}

{% block body %}

    <div class="container-fluid">
        <div class="row">
            {% for item in balance|filter(item => item.amount|number_format(8) > 0) %}
                {% include "crypto/_crypto_template.html.twig" with {'exchange_path': 'crypto_trades_exchange_kukoin' } %}
            {% endfor %}
        </div>
    </div>

{% endblock %}
